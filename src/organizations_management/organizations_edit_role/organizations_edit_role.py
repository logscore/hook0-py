# generated by borea

# if you want to edit this file, add it to ignores in borea.config.json, glob syntax

# TODO: not implemented

from typing import Any, TYPE_CHECKING

if TYPE_CHECKING:
    from ...hook0_api import Hook0API


class OrganizationsEditRole:
    def __init__(self, parent: "Hook0API"):
        self.parent = parent

    def organizations_edit_role(
        self,
        organization_id: str,
        role: str,
        user_id: str,
    ) -> Any:
        """
        Change the role of a user that has already access to an organization you have write access to.

        Args:
            organization_id:
            role: No description provided
            user_id: No description provided

        Returns:
            Response data
        """
        path = f"/api/v1/organizations/{organization_id}/invite"
        params = {}
        headers = {}
        json_data = {
            "role": role if role is not None else None,
            "user_id": user_id if user_id is not None else None,
        }
        json_data = {k: v for k, v in json_data.items() if v is not None}

        response = self.parent._make_request(
            method="PUT",
            path=path,
            params=params,
            headers=headers,
            json_data=json_data,
        )
        return response.json()
